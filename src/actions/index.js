import * as types from "../constants/ActionTypes.js";

// let nextMessageId = 0;
// const nextUserId = 0;
//
// export const addMessage = (message, username, userid) => {
// 	return {
// 		type: types.ADD_MESSAGE,
// 		id: nextMessageId++,
// 		message: message,
// 		username: username,
// 		userid: userid,
// 	};
// };
//
// export const receiveMessage = (message, username, userid) => {
// 	return {
// 		type: types.RECEIVE_MESSAGE,
// 		id: nextMessageId++,
// 		message: message,
// 		username: username,
// 		userid: userid,
// 	};
// };

// export const addUser = (name) => ({
// 	type: types.ADD_USER,
// 	id: nextUserId++,
// 	name
// });
//
// export const populateUsersList = (users) => ({
// 	type: types.USERS_LIST,
// 	users
// });